import LaunchList from './LaunchList';
import LaunchListFilters from './LaunchListFilters';

const Main = () => {
  return (
    <main>
      <LaunchListFilters />
      <LaunchList />
    </main>
  );
};

export default Main;
